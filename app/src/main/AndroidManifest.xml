<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.asistem.pcs_uas_0125">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="id.asistem.pcs_uas_0125.LiveScore"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="pcs_uas_0125"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LiveScore">
        <activity android:name="id.asistem.pcs_uas_0125.ui.detail.EventDetailActivity"/>
        <activity android:name="id.asistem.pcs_uas_0125.ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>